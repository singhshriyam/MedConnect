<div id="message-<%= message.id %>">
  <small><strong><%= message.user.first_name.capitalize %></strong>
    <i><%= message.created_at.in_time_zone("Europe/Berlin").strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>
