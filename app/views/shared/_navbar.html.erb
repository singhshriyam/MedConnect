<div class="navbar">
  <div>
    <span class="med">Med</span><span class="connect">Connect</span>
  </div>
  <div>
    <%= link_to "Home", root_path %>
    <%= link_to "About", about_path %>
    <% if user_signed_in? %>
      <%= link_to "Profile", profile_path %>
    <% else %>
      <%= link_to "Profile", new_user_session_path %>
    <% end %>
    <%= link_to "Get in touch", getintouch_path %>
    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete, turbo: true }, rel: "nofollow" %>
    <% else %>
      <%= link_to "Login", new_user_session_path %>
    <% end %>
  </div>
</div>
